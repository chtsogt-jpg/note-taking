# Note Taking App

A simple, self-contained note-taking tool built as a single HTML file. No server required — just open `index.html` in your browser.

## Features (Planned)

- Create, edit, and delete notes
- Search and filter notes
- Tag notes for organization
- Local storage persistence
- Export notes
- Dark mode
- Markdown rendering

## Usage

Open `index.html` in any modern browser. All data is saved to your browser's localStorage.
